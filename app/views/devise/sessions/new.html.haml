.center.no-data
  .row
    .col.s6.offset-s3
      = image_tag('job.png', class: "no-data-image")
      %h5 Log in with given email and password
      or
      = link_to "sign up", new_user_registration_path
      with your own information.
      .col.s6.offset-s3
        = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          = f.error_notification
          .form-inputs.devise-flat-button
            = f.text_field :email, required: false, autofocus: true, placeholder: "Email", value: "example@example.com"
            = f.text_field :password, required: false, placeholder: "Password", value: "password"
          .form-actions
            = f.button :submit, "Log in", class: "btn z-depth-0 blue-color"
        .devise-flat-button
          = render "devise/shared/links"